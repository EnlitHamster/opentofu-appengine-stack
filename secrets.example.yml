# This file needs to be configured per installation.
# Each secret refers to some kind of access token that cannot be checked into the codebase.
# Make sure to duplicate this file and rename it to `secrets.yml`, and setting a value for
#  the secrets present.

# Follow the guide below to configure the Cloud Build connection to the source code repository.
# https://cloud.google.com/build/docs/automating-builds/github/connect-repo-github?generation=2nd-gen#connecting_a_github_host_programmatically
github_personal_access_token: ...